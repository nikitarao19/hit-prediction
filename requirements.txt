# Runtime dependencies for the Streamlit dashboard only
# (src/what_makes_a_hit/dashboard/app.py). The dashboard loads precomputed
# processed data/model artifacts and does live inference for the feature
# explorer, so it needs scikit-learn + xgboost to unpickle and call .predict()
# on the trained models, but it never runs the Kaggle download or SHAP
# computation itself -- those are pipeline-only (see requirements-dev.txt).
pandas>=2.1,<3
numpy>=1.26,<3
pyarrow>=15,<26
plotly>=5.20,<6
streamlit>=1.35,<2
scikit-learn>=1.4,<2
xgboost>=2.0,<3
joblib>=1.3,<2
